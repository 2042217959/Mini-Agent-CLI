{
  "name": "mini-agent",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "build": "bun build src/cli.ts --compile --minify --outfile=mini-agent"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "typescript": "^5.7.0"
  }
}
